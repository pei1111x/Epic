<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <title>Epic Online</title>
    <style>
        body {
            background-color: #ffc1e396;
        }

        .custom-btn {
            background-color: #e9b0ffce;
            color: #fff;
            border-color: #e9b0ffce;
        }

        .custom-btn:hover {
            background-color: #e9b0ff;
            border-color: #e9b0ff; 
        }

        .custom-btn a {
        color: white; 
        text-decoration: none;
        }
    </style>
</head>
<body>
    <%- include('partials/header') %>
    <div class="container">
        <br>
        <div class="text-center"><h2>Items In Cart</h2></div>
        <br>
      
        <table class="table table-hover small text-center">
          <thead>
              <tr>
                <th width="100">Product Name</th>
                <th width="100">Product Image</th>
                <th width="50">Price</th>
                <th width="50">Quantity</th>
                <th width="50">Delete And Edit Product</th>
              </tr>
          </thead>
          <tbody>
            <% for(let i = 0; i < cart.length; i++) { %>
                <tr>
                    <td><%= cart[i].name %></td>
                    <td><img src="images/<%= cart[i].image %>" alt="<%= cart[i].name %>" style="width: 50px; height: 50px;"></td>
                    <td>$<%= cart[i].price %></td>
                    <td><%= cart[i].quantity %></td>
                    <td>
                        <button class="custom-btn">
                            <a href="/editcart/<%= cart[i].cartId %>">Edit</a>
                        </button>
                        <button class="custom-btn">
                            <a href="/delectProduct/<%= cart[i].cartId %>" onclick="return confirm('Are you sure you want to delete this product in the cart?')">Delete</a>
                        </button>
                    </td>
                </tr>
            <% } %>
        </tbody>
        </table>
        <div class="d-flex flex-column align-items-end">
            <div>
                <h6>Total Price: $<%= totalPrice %></h6>
            </div>
            <div>
                <a href="/check" class="btn custom-btn">Check Out</a>
            </div>
        </div>
    </div>

</body>
</html>