<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <title>Epic Online</title>
    <style>
        body {
            background-color: #ffc1e396;
        }

        .custom-btn {
            background-color: #e9b0ffce;
            color: #fff;
            border-color: #e9b0ffce;
        }

        .custom-btn:hover {
            background-color: #e9b0ff;
            border-color: #e9b0ff; 
        }

        form {
            max-width: 400px;
            margin: auto;
            padding: 20px;    
        }
    </style>
</head>

<body>
    <%- include('partials/header') %>
    <br>
    <h1 class="text-center">Edit product</h1>
    <div class="text-center">
        <% if (cart) { %>
            <form action="/editcart/<%= cart.cartId %>" method="POST" enctype="multipart/form-data">
                <label for="name">Product Name:</label>
                <input type="text" class="form-control" id="name" name="name" value="<%= cart.name %>" readonly><br>
    
                <label for="quantity">Quantity:</label>
                <input type="number" class="form-control" id="quantity" name="quantity" min="0" step="1" value="<%= cart.quantity %>" required><br>
    
                <label for="price">Price:</label>
                <input type="number" class="form-control" id="price" name="price" value="<%= cart.price %>" readonly><br>
    
                <label for="image">Image:</label>
                <input type="text" class="form-control" id="image" name="image" value="<%= cart.image %>" readonly><br>
                <img src="/images/<%= cart.image %>" width="20%"><br><br>
    
                <div class="row justify-content-center">
                    <div class="col-auto">
                        <button a href="/" class="custom-btn">Back</button>
                        <button type="submit" class="custom-btn">Update Product</button>
                    </div>
                </div>
            </form>
            <% } else { %>
            <p>No product found.</p>
            <% } %>
    </div>
</body>
</html>