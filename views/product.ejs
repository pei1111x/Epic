<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <title>Epic Online</title>
    <style>
        body {
            background-color: #ffc1e396;
        }

        .custom-btn {
            background-color: #e9b0ffce;
            color: #fff;
            border-color: #e9b0ffce;
        }

        .custom-btn:hover {
            background-color: #e9b0ff;
            border-color: #e9b0ff; 
        }
    </style>
</head>

<body>
    <%- include('partials/header') %>
    <div class="container">
        <br>
        <div class="text-center"><h1>Product Details</h1></div>
        <br>
        <div class="text-center">
            <% if (product) { %> <!-- Check if product data exists -->
                <div>
                    <p><img src="/images/<%=product.image%>" width="20%"></p>
                    
                    <h4> <%= product.productName %></h4>
                    <p><b>Price:</b> $<%= product.price.toFixed(2) %></p>
                    <p>
                        <b>Category:</b> <%= product.category %> 
                        <b>Quantity:</b> <%= product.quantity %> 
                        <b>Sold:</b> <%= product.sold %>
                    </p>
                    <p><b>Description:</b><br> <%= product.description %></p> 
                </div>
            <% } else { %>
                <p>No student found.</p>
            <% } %>
            
            <div class="row justify-content-center">
                <div class="col-auto">
                    <a href="/" class="btn custom-btn">Back</a>
                </div>
                
            </div>
        </div>
    </div>

</body>
</html>