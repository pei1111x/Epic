<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <title>Epic Online</title>
    <style>
        body {
            background-color: #ffc1e396;
        }

        .custom-btn {
            background-color: #e9b0ffce;
            color: #fff;
            border-color: #e9b0ffce;
        }

        .custom-btn:hover {
            background-color: #e9b0ff;
            border-color: #e9b0ff; 
        }

        .custom-btn {
            background-color: #e9b0ffce;
            color: #fff;
            border-color: #e9b0ffce;
        }

        .custom-btn:hover {
            background-color: #e9b0ff;
            border-color: #e9b0ff;
            color: black; 
        }

        .custom-btn a {
            color: white; 
            text-decoration: none;
        }

        form {
            max-width: 400px;
            margin: auto;
            padding: 20px;    
        }
    </style>
</head>

<body>
    <%- include('partials/header') %>
    <div class="container">
        <br>
        <h1 class="text-center">Add New Products</h1>
        <div class="text-center"><br>
            <% if (product) { %> 
                <form action="/cart" method="POST" enctype="multipart/form-data">
                    <label for="name">Product Name:</label>
                    <input type="text" id="name" class="form-control" name="name" value ="<%= product.productName %>" readonly ><br>
    
                    <label for="quantity">Quantity:</label>
                    <input type="number" id="quantity" class="form-control" name="quantity" min="0" step="1" required><br>
    
                    <label for="price">Price:</label>
                    <input type="number" id="price" class="form-control" name="price" value="<%= product.price %>" readonly><br>
    
                    <label for="image">Image:</label>
                    <input type="text" id="image" class="form-control" name="image" value="<%= product.image %>" readonly><br>
                    <img src = "/images/<%= product.image %>" width="150px"><br><br>
                    
                    <button type="submit" class="custom-btn">Add Product To Cart</button>
                    <div style="margin-top: 5px;">
                        <a href ="/" class="btn custom-btn">Back</a>
                    </div>
                </form>
            <% } else { %>
                <p>No product found.</p>
            <% } %>
            
        </div>
</body>
</html>
    